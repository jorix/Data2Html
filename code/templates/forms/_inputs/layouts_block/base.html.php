<?php exit("It's uncooked!"); ?>
$${label-width = 2}
$${_level-0?[[
    <div class="form-group row">
        <label for="$${id}" class="col-sm-1 col-form-label">$${title}</label>
        <div class="col-sm-$${visual-width}">$${body}</div>
    </div>
]]:[[
    <label for="$${id}" class="col-sm-1 col-form-label">$${title}</label>
    <div class="col-sm-$${visual-width}">$${body}</div>
]]}
