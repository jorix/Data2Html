<?php die("It's uncooked!"); ?>
<div data-d2h-message="#$${id}"></div>
<input type="checkbox"
    placeholder="$${description}"
    name="$${name}"
    id="$${id}"
    data-d2h-from-id="$${from-id}"
    value="1"
    $${validations}
>