<?php die("It's uncooked!"); ?>
<input type="text" class="form-control"
    placeholder="$${description}"
    id="$${id}"
    name="$${name}"
    data-d2h-from="$${from-id}"
    value=""
    $${validations}
>
